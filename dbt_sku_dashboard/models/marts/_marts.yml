version: 2
models:
  - name: fct_sku_profit
    description: All-time profit per canonical SKU, with net/gross/MSRP margin denominators.
    columns:
      - name: sku
        tests: [not_null, unique]
  - name: fct_sku_cost_watch
    description: Forward-looking pricing signal. Compares cost-at-sale to current LastCost; flags margin erosion.
    columns:
      - name: sku
        tests: [not_null, unique]
